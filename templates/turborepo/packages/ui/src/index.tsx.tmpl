export function Button({ children }: { children: React.ReactNode }) {
  return (
    <button style={{ padding: "8px 16px", borderRadius: 6, border: "1px solid #333", cursor: "pointer" }}>
      {children}
    </button>
  );
}
