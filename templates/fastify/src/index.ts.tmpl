import Fastify from 'fastify'

const app = Fastify({ logger: true })

app.get('/', async () => {
  return { message: 'Hello from Fastify. SkipInit template.', project: '{{projectName}}' }
})

app.listen({ port: 3000 }).catch((err) => {
  app.log.error(err)
  process.exit(1)
})
