import { Component } from '@angular/core';
import { CommonModule } from '@angular/common';

@Component({
  selector: 'app-root',
  standalone: true,
  imports: [CommonModule],
  template: `<main><h1>{{projectName}}</h1><p>SkipInit Angular template.</p></main>`
})
export class AppComponent {}
